# LinkedIn Post

I wanted a SIEM project that looked and behaved like something an engineer actually built, not a generic clone of a dashboard template.

So I rebuilt my project under one identity:

`[0xchou00]`

The result is **0xchou00 platform**: a Linux-ready SIEM built around real telemetry flow, real detections, and tamper-evident storage.

What it does:

- ingests SSH and web logs
- normalizes raw lines into a consistent event schema
- detects brute-force and suspicious web behavior
- loads YAML-based detection rules
- flags real frequency spikes with a functional anomaly detector
- hash-chains logs and alerts for integrity verification
- exposes FastAPI endpoints and a live operator dashboard

The engineering challenge was not just adding features. It was enforcing consistency:

- one architecture
- one detection path
- one identity system
- one operator experience

I also pushed the project visually so it would feel like a genuine flagship build:

- black / white / neon green palette
- terminal-inspired UI
- CRT mascot with a `0x` eye and `>_` prompt
- consistent 0xchou00 branding across deploy assets, README, dashboard, and media

This project represents the kind of work I want to do professionally:

- build detection systems
- design SIEM pipelines
- improve SOC workflows
- turn security ideas into installable tools

GitHub:
https://github.com/0xchou00

If you work in Blue Team, SOC, or detection engineering, I’d be glad to connect.

#0xchou00 #Cybersecurity #BlueTeam #SOC #SIEM #DetectionEngineering #FastAPI #Python #SecurityEngineering #ThreatDetection
